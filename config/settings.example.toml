[gmail]
credentials_file = "config/credentials.json"
token_file = "config/token.json"
timezone = "America/Bogota"
senders = [
  "auxmebuca@enviacolvanes.com.co",
  "reexbuca@enviacolvanes.com.co"
]

[paths]
attachments_dir = "data/attachments"
output_dir = "data/output"
database_file = "data/database/guias.db"

[excel]
columns = [
  "PLANILLA",
  "SERVICIO",
  "GUIA",
  "UNID",
  "TIPO DE SERVICIO",
  "DESTINATARIO",
  "DIRECCION",
  "MUNICIPIO",
  "VALOR",
  "OPERADOR",
  "ESTADO",
  "CAUSAL",
  "F_INGRESO",
  "F_ENTREGA"
]
editable_columns = ["OPERADOR", "ESTADO", "CAUSAL"]

[oficina]
nombre = "SAN GIL"
admin_name = "JOHAN A. ORTIZ"
# Se le muestran al cliente final cuando consulta una guia que esta en la oficina.
direccion = "Calle 10 # 9-50, San Gil, Santander"
telefono = "300 000 0000"

[servidor]
# HTTPS opcional: si dejas estos campos vacios, el panel sigue sirviendo
# por HTTP plano como siempre. Para activarlo, genera un certificado
# (autofirmado o real) y apunta aqui a los archivos .pem/.key.
cert_file = ""
key_file = ""

# Copia del respaldo fuera del disco del servidor: si el disco del VPS falla,
# los respaldos locales se pierden todos a la vez.
#
# `comando` recibe la ruta del respaldo en {archivo}. Para OneDrive de Office
# 365 se usa rclone, que no exige registrar la aplicacion en Azure:
#   1. apt install rclone   (en el VPS)
#   2. rclone config  -> new remote -> onedrive -> seguir el asistente
#   3. activo = true
# Tambien sirve para otro servidor:  comando = "scp {archivo} usuario@host:/ruta/"
[respaldo_externo]
activo = false
comando = "rclone copy {archivo} onedrive:Respaldos Expresangil"
horas = 24
