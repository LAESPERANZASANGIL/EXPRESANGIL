[gmail]
credentials_file = "config/credentials.json"
token_file = "config/token.json"
timezone = "America/Bogota"
senders = [
  "auxmebuca@enviacolvanes.com.co",
  "reexbuca@enviacolvanes.com.co"
]

[paths]
attachments_dir = "data/attachments"
output_dir = "data/output"
database_file = "data/database/guias.db"

[excel]
columns = [
  "PLANILLA",
  "SERVICIO",
  "GUIA",
  "UNID",
  "TIPO DE SERVICIO",
  "DESTINATARIO",
  "DIRECCION",
  "MUNICIPIO",
  "VALOR",
  "OPERADOR",
  "ESTADO",
  "CAUSAL",
  "F_INGRESO",
  "F_ENTREGA"
]
editable_columns = ["OPERADOR", "ESTADO", "CAUSAL"]

[oficina]
nombre = "SAN GIL"
admin_name = "JOHAN A. ORTIZ"
# Se le muestran al cliente final cuando consulta una guia que esta en la oficina.
direccion = "Carrera 9 # 17-22, San Gil, Santander"
telefono = "3042624407"

[servidor]
cert_file = ""
key_file = ""

# Copia del respaldo en Google Drive, fuera del disco del servidor: si el
# disco del VPS falla, los respaldos locales se pierden todos a la vez.
# Autoriza una sola vez desde el PC con
#   python -m gestor_guias.app respaldo-drive --autorizar
# y copia el token_drive.json resultante al servidor.
[respaldo_drive]
activo = false
carpeta = "WILLIANS, Institucion Educativa La Esperanza"
copias = 30
horas = 24
credentials_file = "config/credentials.json"
token_file = "config/token_drive.json"
